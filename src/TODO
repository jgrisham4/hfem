Problem with the quadrature is fixed.  Now need to create a mesh, and assemble global matrices.
After that, I need to add the fluxes and solve the linear system.  Also going to need to develop
or use a ode solver.
